# Citation Drift Sentinel

## Reproduction Guide
Reproducing this project requires a free Groq API key. Explicitly state the key must be placed in a `.env` file at the project root using the following format:
`GROQ_API_KEY=your_api_key_here`
